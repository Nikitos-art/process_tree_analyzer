# Process Tree Analyzer

This project parses Windows Event Log (.evtx) files to extract  
process-related events and reconstruct parent-child relationships  
between processes.

It provides a summary of event activity and builds a hierarchical  
view of how processes were spawned, making it easier to analyze  
system behavior and identify potentially suspicious execution chains.